import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-choice',
  imports: [RouterLink],
  templateUrl: './choice.html',
  styleUrl: './choice.css',
})
export class Choice {

}
